events {
    worker_connections  16;
}
http {
    upstream balancer.com {
        server .;
        server srv2.example.com;
        server srv3.example.com;
    }

    server {
        listen 80;
        location / {
            proxy_pass http://balancer.com;
        }
    }
}
